package com.partner.contract.agreement.repository;

import com.partner.contract.agreement.domain.AgreementIncorrectPosition;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AgreementIncorrectPositionRepository extends JpaRepository<AgreementIncorrectPosition, Long> {
}
