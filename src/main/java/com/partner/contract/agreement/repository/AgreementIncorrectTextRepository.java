package com.partner.contract.agreement.repository;

import com.partner.contract.agreement.domain.AgreementIncorrectText;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AgreementIncorrectTextRepository extends JpaRepository<AgreementIncorrectText, Long> {
}
